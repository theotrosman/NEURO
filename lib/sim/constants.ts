// Paso de integracion fijo de la simulacion (ms).
export const DT = 1.0;

// Densidad por defecto (multiplica el numero de neuronas de cada region).
export const DEFAULT_DENSITY = 1.0;

// Ganancia sinaptica global por defecto.
export const DEFAULT_GAIN = 1.0;

// Amplitud del ruido de fondo (mantiene la red viva).
export const DEFAULT_NOISE = 2.6;
